package orsen.models

/** A Token that represents a phrase in a body of text.
  *
  * @constructor create a new Token with an sentenceId, id, and text
  * @param sentenceId the id of the sentence that contains this token
  * @param id the Token's unique id
  * @param text the text that constitutes the Token
  */
class Token(sentenceId: Integer, id: Integer, text: String) {
  // STUB
}


// vim: set ts=4 sw=4 et:
